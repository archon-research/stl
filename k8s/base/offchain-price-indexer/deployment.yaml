apiVersion: apps/v1
kind: Deployment
metadata:
  name: offchain-price-indexer
spec:
  replicas: 1
  # Explicit, matching the API-server default — ArgoCD must own spec.strategy or
  # a later change to it cannot prune rollingUpdate. k8s/AGENTS.md, "Rollout strategy".
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: offchain-price-indexer
  template:
    metadata:
      labels:
        app: offchain-price-indexer
    spec:
      serviceAccountName: offchain-price-indexer
      containers:
        - name: offchain-price-indexer
          image: offchain-price-indexer
          envFrom:
            - configMapRef:
                name: otel-config
            - configMapRef:
                name: offchain-price-indexer
            - secretRef:
                name: offchain-price-indexer
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
      terminationGracePeriodSeconds: 60
