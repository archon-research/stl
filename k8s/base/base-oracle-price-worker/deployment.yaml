apiVersion: apps/v1
kind: Deployment
metadata:
  name: base-oracle-price-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: base-oracle-price-worker
  template:
    metadata:
      labels:
        app: base-oracle-price-worker
    spec:
      serviceAccountName: base-oracle-price-worker
      containers:
        - name: base-oracle-price-worker
          # Distinct image key — overlay maps this to the oracle-price-worker ECR repo.
          # Same binary as oracle-price-worker; chain is selected via CHAIN_ID env var.
          image: base-oracle-price-worker
          envFrom:
            - configMapRef:
                name: otel-config
            - configMapRef:
                name: base-oracle-price-worker-config
            - secretRef:
                name: base-oracle-price-worker
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -f oracle_price_worker"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 60
