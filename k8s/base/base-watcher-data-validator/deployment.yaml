apiVersion: apps/v1
kind: Deployment
metadata:
  name: base-watcher-data-validator
spec:
  replicas: 1
  selector:
    matchLabels:
      app: base-watcher-data-validator
  template:
    metadata:
      labels:
        app: base-watcher-data-validator
    spec:
      serviceAccountName: base-watcher-data-validator
      containers:
        - name: base-watcher-data-validator
          # Image name used as the Kustomize images transformer key.
          # The overlay kustomization.yaml overrides newName + newTag.
          image: base-watcher-data-validator
          env:
            # Inject the deployment name so this validator registers its own
            # Temporal schedule/task queue (cfg.Name) distinct from other chains.
            - name: SERVICE_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.labels['app']
          envFrom:
            - configMapRef:
                name: base-watcher-data-validator
            - secretRef:
                name: base-watcher-data-validator
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
      terminationGracePeriodSeconds: 60
