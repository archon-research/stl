apiVersion: apps/v1
kind: Deployment
metadata:
  name: base-watcher
spec:
  replicas: 0
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: base-watcher
  template:
    metadata:
      labels:
        app: base-watcher
    spec:
      serviceAccountName: base-watcher
      containers:
        - name: base-watcher
          # Image name used as the Kustomize images transformer key.
          # The overlay kustomization.yaml overrides newName + newTag.
          image: base-watcher
          args: ["--enable-traces=false"]
          envFrom:
            - configMapRef:
                name: otel-config
            - configMapRef:
                name: base-watcher-config
            - secretRef:
                # Populated by ExternalSecret — secret name mirrors the service.
                name: base-watcher
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -f watcher"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 60
