apiVersion: apps/v1
kind: Deployment
metadata:
  name: optimism-sparklend-position-tracker
spec:
  replicas: 1
  # Explicit, matching the API-server default — ArgoCD must own spec.strategy or
  # a later change to it cannot prune rollingUpdate. k8s/AGENTS.md, "Rollout strategy".
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 25%
      maxUnavailable: 25%
  selector:
    matchLabels:
      app: optimism-sparklend-position-tracker
  template:
    metadata:
      labels:
        app: optimism-sparklend-position-tracker
    spec:
      serviceAccountName: optimism-sparklend-position-tracker
      containers:
        - name: optimism-sparklend-position-tracker
          image: optimism-sparklend-position-tracker
          envFrom:
            - configMapRef:
                name: otel-config
            - configMapRef:
                name: optimism-sparklend-position-tracker-config
            - secretRef:
                name: optimism-sparklend-position-tracker
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -f sparklend"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 90
