apiVersion: apps/v1
kind: Deployment
metadata:
  name: arbitrum-backup-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: arbitrum-backup-worker
  template:
    metadata:
      labels:
        app: arbitrum-backup-worker
    spec:
      serviceAccountName: arbitrum-backup-worker
      containers:
        - name: arbitrum-backup-worker
          image: arbitrum-backup-worker
          envFrom:
            - configMapRef:
                name: otel-config
            - configMapRef:
                name: arbitrum-backup-worker-config
            - secretRef:
                name: arbitrum-backup-worker
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -f raw-data-backup"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 60
