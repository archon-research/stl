apiVersion: apps/v1
kind: Deployment
metadata:
  name: arbitrum-watcher
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app: arbitrum-watcher
  template:
    metadata:
      labels:
        app: arbitrum-watcher
    spec:
      serviceAccountName: arbitrum-watcher
      containers:
        - name: arbitrum-watcher
          # Image name used as the Kustomize images transformer key.
          # The overlay kustomization.yaml overrides newName + newTag.
          image: arbitrum-watcher
          args: ["--enable-traces=false"]
          env:
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://localhost:4317"
            - name: JAEGER_ENDPOINT
              value: "localhost:4317"
          envFrom:
            - configMapRef:
                name: arbitrum-watcher-config
            - secretRef:
                # Populated by ExternalSecret — secret name mirrors the service.
                name: arbitrum-watcher
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "100m"
              memory: "128Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -f watcher"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 60
