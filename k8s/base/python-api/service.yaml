apiVersion: v1
kind: Service
metadata:
  name: python-api
  annotations:
    tailscale.com/expose: "true"
    tailscale.com/hostname: "python-api"
    tailscale.com/tags: "tag:k8s-external"
spec:
  selector:
    app: python-api
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
