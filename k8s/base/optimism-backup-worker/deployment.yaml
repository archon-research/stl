apiVersion: apps/v1
kind: Deployment
metadata:
  name: optimism-backup-worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: optimism-backup-worker
  template:
    metadata:
      labels:
        app: optimism-backup-worker
    spec:
      serviceAccountName: optimism-backup-worker
      containers:
        - name: optimism-backup-worker
          image: optimism-backup-worker
          envFrom:
            - configMapRef:
                name: otel-config
            - configMapRef:
                name: optimism-backup-worker-config
            - secretRef:
                name: optimism-backup-worker
          resources:
            requests:
              cpu: "50m"
              memory: "64Mi"
            limits:
              cpu: "200m"
              memory: "256Mi"
          livenessProbe:
            exec:
              command: ["/bin/sh", "-c", "pgrep -f raw-data-backup"]
            initialDelaySeconds: 30
            periodSeconds: 30
            failureThreshold: 3
      terminationGracePeriodSeconds: 60
