# =============================================================================
# Compute Module - Main Entry Point
# =============================================================================
# This module consolidates all compute layer resources.
# Source files: ecs.tf, ecs_watcher.tf, ecs_backup_worker.tf

# All compute resources are defined in separate .tf files in this directory
# and are loaded automatically by Terraform when this directory is used as a module.
