# =============================================================================
# Operations Module - Main Entry Point
# =============================================================================
# This module consolidates all operations layer resources.
# Source files: backend.tf, bastion.tf, iam_ecs_task.tf, monitoring.tf,
#               outputs.tf, redis_monitoring.tf, secrets.tf, tigerdata_users.tf

# All operations resources are defined in separate .tf files in this directory
# and are loaded automatically by Terraform when this directory is used as a module.
