line-length = 120
target-version = "py312"

[lint]
select = ["E", "F", "I"]
ignore = []

[lint.per-file-ignores]
# Vendored axis-synome docstrings carry long single-line provenance notes.
"app/risk_engine/_vendored_synome/**" = ["E501"]
