# stl-verify python

FastAPI service for SparkLend position risk calculations.

## Setup

```bash
uv sync
```

## Run

```bash
uv run uvicorn app.main:app --reload
```

## Test

```bash
uv run pytest
```
