import { css } from '#styled-system/css';

export const filterFieldClassName = css({
  display: 'grid',
  gap: '1',
  minWidth: '0',
});
export const filterLabelClassName = css({
  fontSize: 'xs',
  textTransform: 'uppercase',
  letterSpacing: 'widest',
  color: 'text.muted',
});
