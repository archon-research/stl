// Package maple holds the Maple Finance domain entities for the GraphQL
// indexer: pool/loan/strategy registry rows and their per-cycle state
// snapshots, plus the shared validation helpers (including the load-bearing
// synced_at normalization). Like the parent entity package, it has no
// dependencies outside the standard library.
package maple
